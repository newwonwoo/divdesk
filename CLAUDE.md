# CLAUDE.md — DivDesk 작업 규칙

이 파일은 Claude Code가 세션 시작마다 읽는다. 여기 적힌 건 협상 대상이 아니다.

## 이 앱이 뭔가

배당 ETF만 다루는 매수 의사결정 도구.
① 금액 → 세후 월배당 ② 목표 월배당 → 필요 금액 역산 ③ 지금이 매수 타점인지 점수화
④ 매수기록(Postgres) ⑤ 배당예상일지·추천알람(2차)

사용자는 바이브코더다. **코드를 낼 때마다 평이한 말로 무슨 일이 벌어지는지 함께 설명한다.**

## 절대 원칙

1. **주가·배당률·분배금은 하드코딩 금지.** 전부 수집값. 화면에는 항상 기준일을 표시한다.
2. **수집 실패는 실패로 노출한다.** 추정값으로 덮지 않는다. 추정이면 `is_estimate=true` + 화면 배지.
3. **세율은 코드에 박지 않는다.** `tax_param` 테이블만 본다. 세법 개정은 SQL로 대응.
4. **403/429는 즉시 중단.** 재시도로 뚫으려 하지 말고 백오프 후 다음 소스로. 차단당하면 앱 전체가 죽는다.
5. **버그는 근본 원인을 제거한다.** try/except로 에러 메시지를 감추는 수정은 반려.
6. **커버드콜 종목(`is_covered_call=true`)은 점수와 별개로 "분배금 ≠ 수익" 배지를 강제로 붙인다.**
7. **화면 구성이 바뀌면 `docs/divdesk-mockup.html`도 같은 커밋에서 갱신한다.** 말로만 설명하고 끝내지 않는다.

## 코드 수정 후 필수 검증 4단계

문법만 통과해도 `NameError`는 안 잡힌다. 매번 4개 다 돌린다.

```bash
make verify     # ① ast.parse ② import ③ 목 실행 ④ pyflakes
```

이 4개를 통과하지 못한 코드는 커밋하지 않는다.

## 작업 순서

`분석 → 설계 → 개발 → 검증 → 피드백`.
개발이 끝나면 **품질확인서**를 `docs/QUALITY-<항목>.md` 로 작성한다.
요구사항 대조표 / 검증 4단계 결과 / 발견한 결함 / 미충족 항목을 반드시 포함한다.

## 진행 상황

`docs/dividend-etf-progress.md` 와 `.json` 이 단일 진실 소스다.
코딩 항목을 완료하면 `.json` 의 `done` 을 바꾸고 `.md` 에 한 줄 남긴다.

## 현재 위치 (2026-08-02)

전 기능 구현·배포 완료. **스코어를 백테스트로 검증했고 그 결과로 재설계했다.**
남은 일과 미해결 결함은 `HANDOFF.md` 3절에 있다.

## 스코어를 건드리기 전에 — 필독

`docs/DESIGN.md` 5절을 먼저 읽어라. 아래는 **검증으로 걸러낸 설계**다.
근거 없이 되돌리면 오늘 한 일이 무의미해진다.

- **200일선 기울기를 타점 점수에 되살리지 마라.** 23/23 종목에서 음(-)이었다.
  **뒤집지도 마라** — 최근 기간 상관이 0 이고, 뒤집으면 구조적으로 무너지는
  종목에 최고점을 준다. 제거는 중립이고 뒤집기는 반대편 베팅이다.
- **골든크로스(MA50/200)를 넣지 마라.** 검증 후 제외했다. 싼 구간에서
  골든크로스가 있는 쪽이 오히려 -3.6%p 낮았다.
- **20일 과열 감점을 되살리지 마라.** U자 비단조라 선형 배점으로 못 담는다.
- **타점 비중(현재 25%)을 올리지 마라.** 가격 위치는 검증 실패, 환율은 검증 불가다.

축의 정의는 이렇다. 이 정의에서 벗어나는 신호는 그 축에 넣지 않는다.

> **품질 = 안정성** (이 상품이 꾸준한가) · **타점 = 얼마나 싸게 사는가**

## 채점기는 한 경로만 쓴다

`engine/asof.py` 가 채점 입력 조립을 전담한다. 프로덕션(`/scores/recompute`)과
백테스트가 **같은 함수**를 쓴다. 따로 만들면 "프로덕션과 다른 채점기를 검증"하는
셈이 된다. 지표는 저장 컬럼이 아니라 **종가에서 그 시점 창으로 재계산**한다 —
저장 `ma200` 이 마지막 날짜치뿐이라 과거 재구성이 불가능했기 때문이다.

look-ahead 차단은 `scripts/test_asof.py` 로 단위 검증한다. 채점 입력을 바꾸면
이 테스트를 반드시 같이 돌려라.

## 배포와 코드 전달

**`git pull` 후 `sudo systemctl restart divdesk` 를 빠뜨리지 마라.** 파이썬은
재시작 전까지 옛 코드를 계속 돈다. 이걸로 한참 헤맸다.

**코드 전달은 git 으로만 한다.** GitHub 웹 업로드는 폴더가 아니라 파일을 끌면
경로가 납작해져 레포 루트에 파일 수십 개가 쏟아지고, 잘못된 `package.json` 이
루트에 생겨 Vercel 빌드가 죽은 적이 있다. 웹 업로드는 파일을 지우지도 못한다.

## 프론트 검증

`cd web && node check.cjs` 를 `make verify` 와 함께 돌려라.
**JSX 안 미선언 식별자는 빌드를 통과하고 화면만 죽인다.** 실제로 `quality`·
`timing`·`facts` 미선언 참조로 타점 탭이 통째로 빈 화면이 됐다. 렌더 중
`ReferenceError` 가 나면 리액트가 그 트리를 버리기 때문이다.

## 화면 문구

**짧게 쓴다.** 툴팁에 긴 설명을 넣으면 반려된다.
백분위보다 **순위**를 쓴다 — "상위 15%" 대신 "과거 낙폭 100건 중 15번째로 큼".
"상위/하위 %" 는 심한 건지 평범한 건지 즉시 읽히지 않는다.

**공휴일 표를 만들지 마라.** 거래일은 수집된 시세 날짜에서 도출한다
(`engine/calendar.py`). 별도 표를 두면 매년 갱신 누락으로 조용히 틀어진다.
그 전에 `python -m collector.probe_kr` 를 EC2에서 돌려 엔드포인트를 확정해야 한다.
`collector/sources/naver_kr.py` 는 `verified=False` 로 잠겨 있다 —
**검증 안 된 값을 DB에 넣는 것보다 실패가 낫다**는 판단이므로 임의로 풀지 않는다.

## 하지 말 것

- 남이 배포한 공개 API를 직접 호출 (수명·레이트리밋·검증 불가). 수집은 이 서버에서 직접 한다.
- 임의 셸을 여는 MCP 도구 만들기 (사실상 루트 위임)
- `.env`, DSN, 개인키를 커밋하거나 로그에 출력
