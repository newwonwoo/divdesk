%% 시스템 구조 — 2026-08-02 배포 완료 상태.
%% 옛 판은 이관 전(IP 43.201.133.119, 국내 수집 미검증) 기준이라 폐기했다.
flowchart TB

  subgraph USER["사용자 (조정승)"]
    APP["DivDesk PWA<br/>홈·계산·시뮬레이션·타점·기록"]
    CC["Claude Code<br/>개발·운영"]
  end

  subgraph VERCEL["▲ Vercel — GitHub push 시 자동 배포"]
    FE["React + Vite<br/>web/src/App.jsx<br/>공통 ⓘ 바텀시트(api.jsx)"]
    PX["vercel.json 프록시<br/>/api/* → EC2:8000<br/>(HTTPS↔HTTP 혼합 차단 회피)"]
  end

  subgraph EC2["EC2 서울 · 43.203.45.227 · i-07471e6ba1aa9a5ea"]
    API["FastAPI (systemd: divdesk)<br/>★ git pull 후 restart 필수"]
    MIG["db/migrate.py<br/>기동 시 스키마 자동 정합"]

    subgraph ENGINE["engine/"]
      ASOF["asof.py ★ 채점 입력 조립<br/>지표를 종가에서 시점별 재계산<br/>프로덕션·백테스트 공용"]
      SC["score.py 채점 0~100"]
      OTH["calc · tax · returns · projection<br/>calendar · dividend · risk · exdrop"]
    end

    CRON["cron<br/>6:30 토스 매수이력<br/>7:00 시세·배당"]
    ADP["collector/ 어댑터<br/>계약검증 · 원문보관<br/>403·429 즉시 중단"]
    DB[("PostgreSQL<br/>etf_master · etf_price_daily<br/>dividend_history · fx_rate<br/>purchase · score_snapshot<br/>tax_param · sync_log · raw_snapshot")]
  end

  subgraph SRC["수집 소스 (인증키 0개)"]
    Y["Yahoo chart API<br/>미국 21 + 국내 10<br/>가격·배당·USDKRW"]
    SA["stockanalysis.com<br/>미국 배당 교차검증"]
    TOSS["토스증권 API<br/>매수이력 동기화"]
    NV["네이버 증권<br/>verified=False 로 잠김<br/>임의로 풀지 말 것"]
  end

  subgraph BT["백테스트 (scripts/)"]
    B1["backtest.py 전체 스코어"]
    B2["bt_price · bt_decomp<br/>bt_variants · bt_gc · bt_blend"]
    B3["test_asof.py<br/>look-ahead 차단 단위검증"]
  end

  APP --> FE
  FE --> PX
  PX -->|"REST"| API
  CC -.->|"SSM Session Manager"| API
  API --> MIG
  API --> ASOF
  ASOF --> SC
  API --> OTH
  ASOF --> DB
  OTH --> DB
  CRON --> ADP
  ADP --> Y
  ADP --> SA
  ADP --> TOSS
  ADP -.-> NV
  ADP -->|"정규화 + 원문 보관"| DB

  B1 --> ASOF
  B2 --> ASOF
  B3 --> ASOF
  DB -.->|"직접 조회"| B1

  DB -.->|"tax_param 조회<br/>세율은 코드에 없음"| OTH
  ADP -.->|"전 소스 실패 시 '데이터 없음'<br/>추정으로 덮지 않음"| FE

  GATE["검증 게이트<br/>make verify (4단계)<br/>web/check.cjs (미선언 참조)"]
  CC --> GATE

  DEF["미해결<br/>· etf_price_daily 에 open·low 컬럼 없음<br/>  → v13 수집기 실행 불가, 배당락 지표 중립<br/>· 국내 종목 중복 적재 의심<br/>· DIVDESK_TOKEN 비어 있음 (API 무인증 노출)"]
  DB -.-> DEF

  classDef ok fill:#DDE6E4,stroke:#0E6F63,color:#0F1E3D
  classDef warn fill:#FBF0EA,stroke:#A63A18,color:#0F1E3D
  classDef core fill:#FFFFFF,stroke:#0F1E3D,color:#0F1E3D
  classDef store fill:#E9EDF2,stroke:#4A5872,color:#0F1E3D

  class Y,SA,TOSS,GATE ok
  class NV,DEF warn
  class API,MIG,ASOF,SC,OTH,CRON,ADP,FE,PX,B1,B2,B3 core
  class DB store
