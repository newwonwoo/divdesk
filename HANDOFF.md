# HANDOFF — Claude Code 첫 세션에서 읽을 문서

갱신 2026-08-02 · 이전 판(2026-07-30)은 이관 전 상태라 전면 교체했다.
**먼저 `CLAUDE.md`, 그다음 `docs/DESIGN.md` 를 읽어라.**
이 문서는 "지금 상태와 당장 할 일"만 담는다.

---

## 0. 지금 상태 한 줄

**전 기능 구현·배포 완료. 스코어를 백테스트로 검증했고, 그 결과에 따라 재설계했다.**
남은 건 아래 미완 목록이며 앞의 둘은 데이터 정합성 문제라 우선순위가 높다.

| 위치 | 주소 |
|---|---|
| 화면 | https://divdesk.vercel.app (GitHub push 시 Vercel 자동 배포) |
| API | http://43.203.45.227:8000 (EC2 고정IP) |
| 저장소 | github.com/newwonwoo/divdesk |
| 서버 접속 | `aws ssm start-session --target i-07471e6ba1aa9a5ea` (CloudShell) |
| 서버 코드 | `/home/divdesk/repo` (소유자 `divdesk`) |

## 1. 배포 절차

```bash
# git 은 divdesk 권한으로 — ssm-user 로 하면 dubious ownership 오류가 난다
sudo -u divdesk bash -c 'cd /home/divdesk/repo && git pull && .venv/bin/python scripts/verify.py'
sudo systemctl restart divdesk          # ★ 재시작해야 새 코드가 적용된다
curl -s -X POST http://127.0.0.1:8000/scores/recompute
```

**`systemctl restart` 를 빠뜨리면 `git pull` 을 해도 옛 코드가 계속 돈다.**
실제로 이 때문에 "고쳤는데 왜 그대로냐" 를 한참 헤맸다. 파이썬은 파일이 바뀌어도
프로세스를 다시 띄우기 전엔 반영되지 않는다.

프론트는 GitHub push 만으로 Vercel 이 다시 빌드한다.

**코드 전달은 반드시 git 으로 한다.** GitHub 웹 업로드는 폴더가 아니라 파일을
끌면 경로가 납작해져 레포 루트에 파일 50여 개가 쏟아진다. 실제로 그 사고가 났고,
그중 잘못된 `package.json` 이 루트에 생겨 Vercel 빌드가 죽었다. 웹 업로드는
파일을 지우지도 못해 복구가 더 어렵다. 로컬 클론에서 commit·push 가 유일하게 안전하다.

## 2. 검증 게이트 — 커밋 전 필수

```bash
make verify                 # ① ast.parse ② import ③ 목 실행 ④ pyflakes
cd web && node check.cjs    # 프론트: 선언 없는 식별자 참조 검사
```

`check.cjs` 는 **JSX 안에서 쓰이는 식별자를 자동 수집**해 선언 여부를 본다.
예전에는 감시할 이름을 손으로 적어 둬서, 목록에 없던 `quality`·`timing`·`facts`
미선언 참조가 그대로 배포돼 **타점 탭이 통째로 빈 화면이 됐다.** 렌더 중
`ReferenceError` 가 나면 리액트가 그 트리를 버린다 — 빌드는 통과하므로 이 검사가
유일한 방어다. 같은 유형(`mode` 미정의)으로 이미 한 번 당한 적이 있다.

정규식 기반이라 완벽하지 않다(그 사고 때 5개 중 3개를 잡았다). **통과 = 안전이 아니다.**

## 3. 미완 — 우선순위 순

### 1순위 · `open`·`low` 컬럼 부재 (데이터 정합성)

`etf_price_daily` 에 `open`·`low` 컬럼이 **없는데** `collector/store.py` 는 그 두
컬럼에 INSERT 한다. 즉 **v13 수집기가 이 DB 에서 정상 실행된 적이 없다.**
먼저 수집이 조용히 실패하고 있는지 확인하라.

```bash
sudo -u divdesk psql divdesk -c \
  "SELECT source, ok, left(message,80), ran_at FROM sync_log ORDER BY ran_at DESC LIMIT 8;"
```

이 때문에 **배당락 회복력 10점이 전 구간 중립**이다(낙폭 계산에 시가가 필요).

**정정(2026-08-03):** `db/migrate.py` 30~33행에 `open`·`low` 의 `ADD COLUMN IF NOT
EXISTS` 가 **이미 들어 있다.** 코드 쪽 할 일은 끝났고 남은 건 서버 작업뿐이다 —
EC2 에서 `python -m db.migrate` 를 돌리고 재수집하면 함께 풀린다.

### 2순위 · 국내 종목 중복 적재 의심

국내 종목의 최근 1년 행 수가 294 로 연간 거래일(약 246)을 넘는다. 중복이 있으면
이동평균·낙폭이 전부 어긋난다. `(ticker,date)` 가 PK 이므로 날짜가 잘못 들어왔을
가능성이 크다.

### 3순위 · API 토큰이 꺼져 있음 (보안)

`.env` 의 `DIVDESK_TOKEN` 이 비어 있어 **API 가 인증 없이 외부에 열려 있다.**
서버 `.env` 에 값을 넣고 Vercel 환경변수 `VITE_API_TOKEN` 에 같은 값을 넣은 뒤
재배포한다. 값에 줄바꿈이 섞이면 fetch 가 실패한다.

### 4순위 · 저장 `ma200` 이 마지막 날짜 1행뿐

`store.py` 는 매일 저장하도록 고쳐졌으나 DB 에 반영되지 않았다. 지금은
`engine/asof.py` 가 종가에서 그 시점 창으로 다시 계산하므로 **점수에는 영향이
없다.** 재수집하면 자연히 채워진다.

### 그 밖

- 리츠·커버드콜 별도 평가 모델 (지금은 경고 배지만)
- 국내 종목 배당 이력이 짧아 3년/5년 CAGR 이 안 나온다
- `scripts/bt_variants.py` 의 `rolling_dd` 가 `engine/asof.py` 의 `_dd_history` 와
  중복 구현 — 통합 대상
- `scripts/bt_report.py` 의 5분위 구간 표시가 원신호(소수)에서 `-0~-0` 으로 찍힘
- ~~레포 루트에 웹 업로드 사고로 생긴 파일~~ → **2026-08-03 해소.** 잔해 16개
  (루트 10 + `engine/` 6)를 삭제했다. `docs/QUALITY-레포정리.md` 참조.
  **"동작에는 영향 없다"는 판단은 틀렸었다** — 잔해 중 `main.py`(내용은 App.jsx)와
  `score.py`(내용은 마크다운)가 `make verify` ①문법을 실패시켜 **커밋 게이트가
  막혀 있었다.** 확장자와 내용이 어긋난 파일은 무해하지 않다.

## 4. 하지 말 것 — 검증으로 걸러낸 것들

**`docs/DESIGN.md` 5절을 반드시 읽어라.** 요약하면:

- **200일선 기울기를 타점 점수에 되살리지 마라.** 23/23 종목에서 음(-)이었고
  어느 기간에서도 양(+)인 적이 없다. 동시에 **부호를 뒤집지도 마라** — 최근 기간
  상관이 0 이라 근거가 없고, 뒤집으면 배당 삭감처럼 구조적으로 무너지는 종목에
  최고점을 주게 된다. 제거는 중립이고 뒤집기는 반대편 베팅이다.
- **골든크로스(MA50/200)를 넣지 마라.** 검증 후 제외했다. 낙폭이 큰 구간에서
  골든크로스가 **있는** 쪽이 오히려 -3.6%p 낮았다. 반등을 확인하고 사면 늦는다.
- **20일 과열 감점을 되살리지 마라.** U자 비단조라 선형 배점으로 담기지 않는다.
- **타점 비중을 다시 올리지 마라.** 타점을 구성하는 두 항목이 검증 실패
  (가격 위치) 또는 검증 불가(환율)다.

## 5. 사용자와 일하는 방식

- 순서: **분석 → 설계 → 개발 → 검증 → 피드백.** 개발이 끝나면 품질확인서를
  `docs/QUALITY-<항목>.md` 로 쓴다(요구사항 대조표 · 검증 4단계 결과 · 발견한
  결함 · 미충족 항목 필수).
- **민감도 분석 선행 → 리워크 최소화 → 과잉코딩 금지.**
- **절차 안내는 한 번에 한 단계씩.** 완료를 확인하고 다음으로.
- **코드는 요청받았을 때만.** 기본은 분석·메모.
- `ㄱㄱ` `rr` `다음` = 성공, 다음으로.
- 사용자는 바이브코더다. 코드를 낼 때마다 **평이한 말로 무슨 일이 벌어지는지**
  함께 설명한다.
- 화면 문구는 **짧게.** 툴팁에 긴 설명을 넣으면 반려된다. 백분위보다
  **순위** 표기를 선호한다 — "상위 15%" 대신 "과거 낙폭 100건 중 15번째로 큼".

---

## 6. 이미 밟은 함정 (다시 밟지 마라)

| 함정 | 사실 |
|---|---|
| TTM 배당금을 "최근 370일"로 자름 | 지급일이 밀리면 1회 더 잡혀 배당률 과대. HDV 3.03%→2.37%. **횟수로 자를 것** |
| 지표를 마지막 날짜에만 저장 | 200일선을 그렇게 저장해 기울기를 못 냈다. 시계열 지표는 매일 저장 |
| dataclass 기본값 필드를 중간에 삽입 | 3번 실패. 새 필드는 반드시 맨 뒤에 |
| 컬럼 추가 후 값 미복구 | 마이그레이션은 컬럼만 만든다. `is_benchmark` 가 전부 false 로 남아 벤치마크가 매수 후보에 섞였다 |
| 튜플 길이 가정 | OHLC 추가로 일봉이 5튜플이 됐는데 월봉은 3튜플이라 수집 전량 실패 |
| 연도 경계로 자르기 | 지급이 밀린 해에 회차가 어긋난다. 횟수로 자를 것 |
| 시장 보정에서 부호 오류 | 빼야 할 걸 더하고 있었다. 실데이터만 봤으면 그럴듯해서 넘어갔을 것 |
| 두 테이블을 한 번에 LEFT JOIN 해 진단 | 카티션 곱으로 행수가 부풀려진다. 테이블별로 먼저 집계할 것 |
| 분배금 지급주기를 사람이 안다고 가정 | 3번 반복된 결함. `infer_pays_per_year()` 로 이력에서 도출 |
| 종목코드를 이름으로 추정 | 476850을 RISE 미국배당100으로 등록했으나 실제는 KoAct 배당성장액티브 |
| 커버드콜 고배당을 좋은 점수로 | 분배금에 옵션 프리미엄·원금환급이 섞인다. 배지 강제 |
| 남이 배포한 공개 API 직접 호출 | 수명·레이트리밋·검증 불가. 수집은 이 서버에서 직접 |

## 7. 세금 모델 요약

| 모드 | 분배금 | 매매차익 |
|---|---|---|
| `US_TAXABLE` 일반·미국상장 | 현지 15% 원천징수 | 양도세 22%, 연 250만 공제, 분리과세 |
| `KR_TAXABLE` 일반·국내상장 | 15.4% 원천징수 | **매매차익도 배당소득으로 15.4%** |
| `KR_SHELTER` 절세계좌 | 과세이연 | 동일 |

연 금융소득 2,000만원 초과 시 종합과세. 워치독은 80% 도달 시 경고.
**세율 상수는 전부 `tax_param` 테이블에서 읽는다. 코드에 숫자를 쓰지 마라.**

## 8. 데이터 소스 현황

| 소스 | 용도 | 상태 |
|---|---|---|
| `query1.finance.yahoo.com/v8/finance/chart/{sym}?events=div` | 미국 가격·배당·USDKRW | 검증 OK, 무인증 |
| `query1.finance.yahoo.com/.../{코드}.KS` | 국내 시세·분배금 | 검증 OK, 원화 |
| `stockanalysis.com/etf/{sym}/dividend/` | 미국 배당 교차검증 | 200 OK |
| `finance.naver.com/api/...` | 국내 교차검증(선택) | **미검증 — `verified=False` 로 잠겨 있다. 임의로 풀지 마라** |
| `data.krx.co.kr` / `dividendhistory.net` | — | 403 / 503, 제외 |

## 9. 실측 데이터 규모 (2026-08-02)

- 가격: 미국 종목 10년 일봉(약 2,550행/종목). 국내는 2022~23년 상장분이 다수
- 배당: **2020-08 이후뿐** — 기성 ETF(SPY·VYM·SDY)조차 그렇다.
  과거 시점에서 3·5년 CAGR 재구성이 불가능한 이유가 이것이다
- 환율: 2021-08 부터 1,302행. 3년 밴드조차 실질 독립 관측이 두어 개
- 종목: 미국 21 + 국내 10 + 벤치마크 4(SPY/VOO/QQQ/069500)
